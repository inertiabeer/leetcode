/**
 * @param {number[]} prices
 * @param {number} fee
 * @return {number}
 */
var maxProfit = function (prices, fee) {
let profit=0;
let buy=Infinity;
let sell=-Infinity;
for(var i=0;i<prices.length;i++)
{
    let item=prices[i];
    
}
};